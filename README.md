# emmet2xml

Уеб приложение за двупосочна трансформация между **Emmet синтаксис** и **XML**, с поддръжка на потребителски акаунти, история на трансформациите, правила за преструктуриране и статистика на документи.

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![PHP](https://img.shields.io/badge/PHP-7.4+-purple.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

---

## 📋 Съдържание

- [Описание](#-описание)
- [Функционалности](#-функционалности)
- [Изисквания](#-изисквания)
- [Инсталация](#-инсталация)
- [Използване](#-използване)
- [Emmet Синтаксис](#-emmet-синтаксис)
- [API Документация](#-api-документация)
- [Структура на проекта](#-структура-на-проекта)
- [База данни](#-база-данни)
- [Конфигурация](#-конфигурация)

---

## 📝 Описание

**emmet2xml** е инструмент за бързо генериране на XML код от Emmet изрази и обратно. Проектът е изграден изцяло с ванилни технологии без използване на външни библиотеки:

- **Frontend**: CSS3, JavaScript (ES6+)
- **Backend**: PHP 7.4+
- **База данни**: SQLite (вградена)

### Какво е Emmet?

Emmet е синтаксис за бързо писане на XML структури. Например:

```
div#container>ul.list>li.item*3{Item $}
```

Се трансформира в:

```xml
<div id="container">
  <ul class="list">
    <li class="item">Item 1</li>
    <li class="item">Item 2</li>
    <li class="item">Item 3</li>
  </ul>
</div>
```

---

## ✨ Функционалности

### Основни трансформации
- ✅ **Emmet → XML** - Конвертира Emmet изрази в форматиран XML код
- ✅ **XML → Emmet** - Обратна трансформация от XML към Emmet синтаксис
- ✅ **Двупосочна работа** - Лесно превключване и размяна на вход/изход

### Настройки за трансформация
- 🔧 **Показване на стойности на елементи** - вкл/изкл
- 🔧 **Показване на стойности на елементи** - вкл/изкл
- 🔧 **Показване на атрибути** - вкл/изкл
- 🔧 **Показване на стойности на атрибути** - вкл/изкл
- 🔧 **Self-closing тагове** - вкл/изкл
- 🔧 **Отстъп** - 2 интервала, 4 интервала или Tab

### Потребителска система
- 👤 **Регистрация** - Създаване на акаунт
- 🔐 **Вход/Изход** - Сигурна автентикация
- 📜 **История** - Запазване на всички трансформации
- 🔄 **Възстановяване** - По настройки, по данни или по двете

### Правила за преструктуриране
- 📐 Дефиниране на правила (напр. `E1+E2 → E2+E1`)
- 🔀 Прилагане върху XML документи
- 💾 Запазване на правила за бъдеща употреба

### Статистика на документи
- 📊 Брой елементи по тип
- 🎨 Топ CSS класове (топ 3, топ 10, топ 100)
- 📋 Брой атрибути
- 📏 Максимална дълбочина на влагане
- 📝 Брой текстови възли

### Извличане на данни
- 📑 От таблици (`<table>`)
- 📝 От списъци (`<ul>`, `<ol>`, `<li>`)
- 🔍 От други структури

---

## 💻 Изисквания

- **PHP** 7.4 или по-нова версия
- **PDO SQLite** разширение (обикновено е включено по подразбиране)
- Уеб браузър с JavaScript поддръжка

---

## 🚀 Инсталация

### 1. Клониране/Копиране на проекта

```bash
cd /път/до/уеб/директория
git clone <repository-url> emmet2xml
# или копирайте файловете ръчно
```

### 2. Проверка на права

Уверете се, че директорията `data/` има права за запис:

```bash
chmod 755 data/
```

### 3. Стартиране с вграден PHP сървър (за разработка)

```bash
cd emmet2xml
php -S localhost:8000
```

### 4. Отваряне в браузър

Отворете: `http://localhost:8000`

### Алтернатива: Apache/Nginx

Просто копирайте файловете в уеб директорията на вашия сървър (напр. `htdocs`, `www`, `public_html`).

---

## 📖 Използване

### Основна трансформация

1. Изберете **посока** на трансформация:
   - `Emmet → XML` - от Emmet израз към XML
   - `XML → Emmet` - от XML към Emmet

2. Въведете текст в **лявия панел**

3. Натиснете **"Трансформирай"** (или `Ctrl+Enter`)

4. Резултатът се появява в **десния панел**

### Настройки

В левия панел с настройки можете да конфигурирате:

| Настройка | Описание |
|-----------|----------|
| Показвай стойности | Включва текстовото съдържание на елементите |
| Показвай атрибути | Включва id, class и други атрибути |
| Показвай стойности на атрибути | Включва стойностите на атрибутите |
| Self-closing | Използва `<tag />` за празни елементи |
| Отстъп | Форматиране на изхода |

### Регистрация и История

1. Натиснете **"Вход"** в горния десен ъгъл
2. Изберете **"Регистрация"** и попълнете данните
3. След вход, всички трансформации се запазват автоматично
4. В таба **"История"** можете да:
   - Преглеждате минали трансформации
   - Възстановявате само настройки
   - Възстановявате само данни
   - Възстановявате и двете

### Правила за преструктуриране

В таба **"Правила"** можете да дефинирате правила за промяна на структурата:

**Пример:**
- Име: `Размени елементи`
- Шаблон: `E1+E2`
- Замяна: `E2+E1`

Това ще размени съседни елементи:
```xml
<!-- Вход -->
<a>link</a><b>bold</b>

<!-- Изход -->
<b>bold</b><a>link</a>
```

### Статистика

В таба **"Статистика"**:
1. Въведете XML код
2. Натиснете **"Анализирай"**
3. Вижте детайлна статистика за документа

---

## 🔤 Emmet Синтаксис

### Основни оператори

| Оператор | Описание | Пример | Резултат |
|----------|----------|--------|----------|
| `>` | Child (дете) | `div>span` | `<div><span></span></div>` |
| `+` | Sibling (съсед) | `div+span` | `<div></div><span></span>` |
| `^` | Climb-up (нагоре) | `div>span^p` | `<div><span></span></div><p></p>` |
| `*` | Multiplication | `li*3` | `<li></li><li></li><li></li>` |
| `()` | Grouping | `div>(p+span)*2` | Групира елементи |

### Атрибути

| Синтаксис | Описание | Пример | Резултат |
|-----------|----------|--------|----------|
| `#id` | ID атрибут | `div#main` | `<div id="main"></div>` |
| `.class` | CSS клас | `div.container` | `<div class="container"></div>` |
| `.c1.c2` | Множество класове | `div.one.two` | `<div class="one two"></div>` |
| `[attr]` | Атрибут | `a[href=#]` | `<a href="#"></a>` |
| `[a=v]` | Атрибут със стойност | `input[type=text]` | `<input type="text">` |

### Текст и номериране

| Синтаксис | Описание | Пример | Резултат |
|-----------|----------|--------|----------|
| `{text}` | Текст | `p{Hello}` | `<p>Hello</p>` |
| `$` | Номериране | `li.item$*3` | `.item1`, `.item2`, `.item3` |
| `$$` | Padding | `li.item$$*3` | `.item01`, `.item02`, `.item03` |

### Примери

```emmet
# Навигация
nav>ul>li*5>a[href=#]{Link $}

# Таблица
table>tr*3>td*4{Cell}

# Форма
form>input[type=text placeholder="Name"]+input[type=email]+button{Submit}

# Сложна структура
div#page>(header>h1{Title})+(main>article*3>h2{Article $}+p{Content})+(footer>p{Copyright})
```

---

## 🔌 API Документация

### Автентикация (`php/auth.php`)

#### Регистрация
```http
POST /php/auth.php?action=register
Content-Type: application/json

{
  "username": "user123",
  "email": "user@example.com",
  "password": "secret123",
  "password_confirm": "secret123"
}
```

#### Вход
```http
POST /php/auth.php?action=login
Content-Type: application/json

{
  "username": "user123",
  "password": "secret123"
}
```

#### Изход
```http
POST /php/auth.php?action=logout
```

#### Проверка на статус
```http
GET /php/auth.php?action=check
```

### История (`php/history.php`)

#### Запазване на трансформация
```http
POST /php/history.php?action=save
Content-Type: application/json

{
  "input_type": "emmet",
  "input_data": "div>p*3",
  "output_data": "<div>...</div>",
  "settings": {
    "showValues": true
  }
}
```

#### Списък с история
```http
GET /php/history.php?action=list&type=all&limit=50&offset=0
```

#### Единичен запис
```http
GET /php/history.php?action=get&id=123
```

### Правила (`php/rules.php`)

#### Запазване на правило
```http
POST /php/rules.php?action=save
Content-Type: application/json

{
  "name": "Swap elements",
  "pattern": "E1+E2",
  "replacement": "E2+E1"
}
```

#### Списък с правила
```http
GET /php/rules.php?action=list
```

### Трансформации (`php/transform.php`)

#### Извличане от таблица
```http
POST /php/transform.php?action=extract-table
Content-Type: application/json

{
  "xml": "<table>...</table>"
}
```

#### Статистика
```http
POST /php/transform.php?action=statistics
Content-Type: application/json

{
  "xml": "<div>...</div>"
}
```

---

## 📁 Структура на проекта

```
emmet2xml/
├── index.php                 # Главна страница (Single Page Application)
├── css/
│   └── style.css            # Всички стилове
├── js/
│   ├── emmet-parser.js      # Emmet лексер, парсер и генератор
│   ├── xml-parser.js        # XML парсер и Emmet генератор
│   ├── transformer.js       # Трансформации и правила
│   ├── statistics.js        # Статистика модул
│   ├── auth.js              # Автентикация (frontend)
│   └── app.js               # Главна логика на приложението
├── php/
│   ├── config.php           # Конфигурация и константи
│   ├── utils.php            # Помощни функции
│   ├── auth.php             # API за автентикация
│   ├── history.php          # API за история
│   ├── transform.php        # API за трансформации
│   └── rules.php            # API за правила
├── includes/
│   └── Database.php         # PDO wrapper за SQLite/MySQL
├── sql/
│   └── schema.sql           # Схема на базата данни
├── data/
│   └── emmet2xml.db         # SQLite база данни (създава се автоматично)
└── README.md                # Тази документация
```

---

## 🗄️ База данни

### Схема

#### users
| Колона | Тип | Описание |
|--------|-----|----------|
| id | INTEGER | Първичен ключ |
| username | VARCHAR(50) | Потребителско име (уникално) |
| email | VARCHAR(100) | Email адрес (уникален) |
| password_hash | VARCHAR(255) | Хеширана парола |
| created_at | DATETIME | Дата на създаване |

#### transformations
| Колона | Тип | Описание |
|--------|-----|----------|
| id | INTEGER | Първичен ключ |
| user_id | INTEGER | FK към users |
| input_type | VARCHAR(10) | 'emmet' или 'xml' |
| input_data | TEXT | Входни данни |
| output_data | TEXT | Изходни данни |
| settings_json | TEXT | Настройки (JSON) |
| created_at | DATETIME | Дата на създаване |

#### refactor_rules
| Колона | Тип | Описание |
|--------|-----|----------|
| id | INTEGER | Първичен ключ |
| user_id | INTEGER | FK към users |
| name | VARCHAR(100) | Име на правилото |
| pattern | TEXT | Шаблон (напр. E1+E2) |
| replacement | TEXT | Замяна (напр. E2+E1) |
| created_at | DATETIME | Дата на създаване |

---

## ⚙️ Конфигурация

Настройките се намират в `php/config.php`:

```php
// Режим на разработка (показва грешки)
define('DEBUG_MODE', true);

// База данни
define('DB_TYPE', 'sqlite');  // 'sqlite' или 'mysql'
define('DB_PATH', __DIR__ . '/../data/emmet2xml.db');

// MySQL (ако се използва)
define('DB_HOST', 'localhost');
define('DB_NAME', 'emmet2xml');
define('DB_USER', 'root');
define('DB_PASS', '');

// Сигурност
define('PASSWORD_MIN_LENGTH', 6);
define('USERNAME_MIN_LENGTH', 3);
define('USERNAME_MAX_LENGTH', 50);
```

---

## 🔒 Сигурност

- ✅ **Password hashing** - Паролите се хешират с `password_hash()` (bcrypt)
- ✅ **Prepared statements** - Защита от SQL injection
- ✅ **XSS защита** - Escape на потребителски данни
- ✅ **Session-based auth** - Сигурни PHP сесии
- ✅ **CSRF** - Препоръчва се добавяне на CSRF токени за продукция

---

## 🐛 Отстраняване на проблеми

### Грешка при свързване с базата данни
```bash
# Проверете дали PDO SQLite е инсталиран
php -m | grep pdo_sqlite

# Проверете права на data/ директорията
chmod 755 data/
```

### Страницата не се зарежда
```bash
# Проверете PHP версията
php -v  # Трябва да е 7.4+

# Стартирайте сървъра
php -S localhost:8000
```

### Трансформацията не работи
- Проверете JavaScript конзолата в браузъра (F12)
- Уверете се, че всички JS файлове са заредени

---

## 📄 Лиценз

MIT License - свободно използване за лични и комерсиални цели.

---

## 👤 Автор

Създадено като учебен проект за демонстрация на:
- Работа с Emmet синтаксис
- Парсване и генериране на XML
- PHP backend без frameworks
- Vanilla JavaScript frontend
- SQLite база данни

---

## 🔮 Бъдещи подобрения

- [ ] Експорт на история (JSON, CSV)
- [ ] Споделяне на трансформации
- [ ] Теми (светла/тъмна)
- [ ] Keyboard shortcuts панел
- [ ] Сравнение на версии
- [ ] WebSocket за real-time preview
- [ ] Плъгин система за допълнителни формати
